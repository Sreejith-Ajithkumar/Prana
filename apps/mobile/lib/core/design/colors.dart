import 'package:flutter/material.dart';

class PranaColors {
  PranaColors._();

  // Brand
  static const Color primary = Color(0xFF2E7D32);
  static const Color primaryLight = Color(0xFF60AD5E);
  static const Color primaryDark = Color(0xFF005005);

  // Secondary
  static const Color secondary = Color(0xFF26A69A);

  // Accent
  static const Color accent = Color(0xFFFFB300);

  // Background
  static const Color background = Color(0xFFF8FAF8);
  static const Color surface = Colors.white;

  // Status
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xFFD32F2F);

  // Text
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF616161);

  // Border
  static const Color border = Color(0xFFE0E0E0);

  // Dashboard
  static const Color calories = Color(0xFFE53935);
  static const Color protein = Color(0xFF3949AB);
  static const Color carbs = Color(0xFFFF9800);
  static const Color fat = Color(0xFFFDD835);
  static const Color water = Color(0xFF29B6F6);
}